#pragma once
#include <string>
using std::size_t, std::string;

int zeichenkette_suchen_rekursiv(string text, string zkette, size_t text_pos = 0, size_t text_such_pos = 0, size_t zkette_such_pos = 0);